# Dev GLB — placeholder

Generar el modelo de prueba:

```bash
cd apps/web
node ../../tools/glb-viewer/scripts/generate-placeholder.mjs
```

Salida: `placeholder-shipyard-preview.glb` (original del repo, uso solo desarrollo).
