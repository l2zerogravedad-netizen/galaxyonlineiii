import { Go3Frame } from '../Go3Frame';

export const metadata = { title: 'Galaxia — Galaxy Online 3' };

// Sistema de galaxia (mapa estelar, sonda, atacar).
export default function Go3GalaxyPage() {
  return <Go3Frame section="galaxy" />;
}
