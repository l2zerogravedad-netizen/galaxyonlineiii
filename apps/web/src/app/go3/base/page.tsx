import { Go3Frame } from '../Go3Frame';

export const metadata = { title: 'Base espacial — Galaxy Online 3' };

// Base espacial (grilla isométrica, edición y asalto a base).
export default function Go3BasePage() {
  return <Go3Frame section="basespace" />;
}
