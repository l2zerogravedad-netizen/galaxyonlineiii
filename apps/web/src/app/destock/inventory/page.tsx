import { InventoryScreen } from '@/features/destock-space/screens/InventoryScreen';

export const metadata = { title: 'DESTOCK SPACE — Inventario' };

export default function Page() {
  return <InventoryScreen />;
}
