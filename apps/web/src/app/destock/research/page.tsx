import { ResearchScreen } from '@/features/destock-space/screens/ResearchScreen';

export const metadata = { title: 'DESTOCK SPACE — Investigación' };

export default function Page() {
  return <ResearchScreen />;
}
