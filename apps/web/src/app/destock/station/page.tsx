import { StationScreen } from '@/features/destock-space/screens/StationScreen';

export const metadata = { title: 'DESTOCK SPACE — Estación' };

export default function Page() {
  return <StationScreen />;
}
