import { MarketplaceScreen } from '@/features/destock-space/screens/MarketplaceScreen';

export const metadata = { title: 'DESTOCK SPACE — Mercado' };

export default function Page() {
  return <MarketplaceScreen />;
}
