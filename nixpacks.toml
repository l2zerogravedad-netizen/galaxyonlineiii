[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "cd packages/shared && npx tsc",
  "cd apps/web && npm run build"
]

[start]
cmd = "cd apps/web && node .next/standalone/server.js"
